# Terraform baseline

This folder intentionally contains no live cloud resources yet. Before the first resource is added, record the target AWS account/region, remote state policy, staging URL, budget alarm, and rollback owner in an ADR and PR.
