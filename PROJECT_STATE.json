{
  "schema_version": "plug-state/1",
  "product": "PLUG",
  "updated_at": "2026-09-17T19:05:00+01:00",
  "updated_by": "person_one",
  "current_phase": "P0",
  "current_step": "P0-device-verify",
  "current_step_title": "Run the app on a real iPhone against a live cloudflared tunnel (mechanism proven 2026-09-17: curl + full Bruno suite passed through a real trycloudflare.com URL, see evidence/P0/logs/tunnel-checkpoint-2026-09-17.log)",
  "manual_section": "27.1",
  "readme": "docs/phases/P0-ONE.md",
  "owner": "person_one",
  "blocked_on": null,
  "last_gate_passed": null,
  "next_gate": "G0",
  "gate_log": [],
  "contracts": {
    "GET /health": "draft",
    "GET /health/ready": "draft",
    "POST /v1/requests": "draft"
  },
  "feature_flags": {},
  "next_actions": [
    { "id": "P0-device-verify", "owner": "person_one",
      "task": "Start the backend + a cloudflared tunnel (see docs/decisions/ADR-004-defer-aws-use-tunnel.md), set PLUG_API_URL in the Xcode scheme to that tunnel's URL, run on a real iPhone, confirm the Engineering tab renders status/version/request ID",
      "manual_section": "27.1 / P0.S7", "acceptance": "Real device over cellular (not the same WiFi) reaches the tunnel; screenshot attached to evidence/P0/ios/." },
    { "id": "P0.S1-two", "owner": "person_two",
      "task": "Clone on Windows and prove docs/onboarding/windows.md works end to end from a clean machine; once proven, run the Bruno collection against a live tunnel URL Person One shares",
      "manual_section": "27.1", "acceptance": "Every documented command runs without an undocumented prerequisite; bru run tests/api passes against the shared tunnel URL." },
    { "id": "P0-connected-checkpoint", "owner": "person_one",
      "task": "With Person Two live: Person One's iPhone hits the tunnel, Person Two runs Bruno against the same tunnel URL from Windows at the same time, compare the request ID in the iOS log against the backend terminal log, commit evidence, then append the signed G0 gate_log entry",
      "manual_section": "27.1", "acceptance": "G0 signed in PROJECT_STATE.json with both signatures and an evidence path." }
  ],
  "do_not_touch": ["/ios/**", "/infra/**", "/backend/src/main/java/app/plug/security/**"],
  "open_risks": [
    "GET /health, GET /health/ready and POST /v1/requests are implemented and tested but not yet frozen through a joint two-person contract PR review — mark frozen@0.1.0 once both engineers have signed off. Contract version was 0.3.0 vs CHANGELOG.md's 0.1.0 (a real inconsistency, since fixed to 0.1.0 on both sides).",
    "GET /health/ready is permitAll at the Spring Security layer, matching the manual's own reference implementation; it still needs to be kept off the public internet at the infra/WAF layer before staging is exposed (see docs/security/threat-model.md).",
    "AWS staging is deferred (ADR-004) — no budget available right now, even for the near-$0/month minimal design in docs/runbooks/aws-staging-setup.md. The Phase 0 connected checkpoint uses a cloudflared Quick Tunnel instead; revisit AWS no later than Phase 3, when Twilio needs a stable (non-rotating) webhook URL a Quick Tunnel can't provide.",
    "Backend has no Redis dependency or Redis readiness check, even though infra/compose.yml and docker-compose.yml provision Redis. Either wire it in when something actually needs it, or drop it from Compose — don't let 'provisioned but unused' stand as if it were tested.",
    "iOS is build- and test-verified in Simulator (xcodebuild test -scheme Plug passes; the app installs and launches on iPhone 17 Simulator; design/generated/Tokens.swift is referenced directly from the Plug target, no duplicate copy) — but not yet run on a physical device, which the manual requires before Phase 0 closes ('the Simulator hides real network behaviour').",
    "docs/onboarding/windows.md has not been run on a real Windows machine yet — person_two (@uzom-a) owns proving it, since she's the one actually on Windows.",
    "docs/design/figma-implementation-checklist.md exists as a checklist document, not linked to an actual Figma file — no Figma access was available this session.",
    "The G0 gate itself cannot be signed by automation: manual.docx §4.3/§27.1 requires a live, two-person connected checkpoint against real staging with a physical iPhone — that is the one remaining step that is inherently not automatable."
  ],
  "environments": {
    "staging": "ephemeral — cloudflared Quick Tunnel started per checkpoint session, see ADR-004; no standing URL",
    "production": "not-yet-provisioned"
  }
}
